import React from 'react'
import {
  Text,
  View
} from 'react-native'

const CategoriesList = (props) => {
  return (
    <View>
      <Text>{props.title}</Text>
    </View>
  )
}

export default CategoriesList
